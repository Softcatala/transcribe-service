[project]
name = "transcriptor"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "nose2",
  "ruff",
  "jiwer",
  "evaluate"
]

[tool.uv.workspace]
members = ["transcribe-core", "transcribe-batch", "transcribe-service"]
